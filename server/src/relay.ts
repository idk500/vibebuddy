/**
 * Event Relay — OpenCode event to Andon state transformation
 *
 * Event mapping is handled in:
 * - opencode.ts (SDK-based, for direct OpenCode SSE stream)
 * - opencode-plugin/index.js (plugin-based, for HTTP adapter API)
 *
 * This module is kept as a placeholder for future relay orchestration logic.
 */
